services:
  - type: web
    name: innovation-backend
    env: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: alembic upgrade head && gunicorn src.api.main:app -w 4 -k uvicorn.workers.UvicornWorker
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: innovation-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: innovation-redis
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_ORIGINS
        value: "*"
      - key: PYTHONPATH
        value: "src"
      - key: GEMINI_API_KEYS
        sync: false
      - key: MP_ACCESS_TOKEN
        sync: false

  - type: web
    name: innovation-frontend
    env: node
    rootDir: frontend
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: API_URL
        fromService:
          type: web
          name: innovation-backend
          property: url
      - key: NEXT_PUBLIC_API_URL
        fromService:
          type: web
          name: innovation-backend
          property: url
      - key: NODE_ENV
        value: production
      - key: NEXT_PUBLIC_SUPABASE_URL
        sync: false
      - key: NEXT_PUBLIC_SUPABASE_ANON_KEY
        sync: false
      - key: SUPABASE_SERVICE_ROLE_KEY
        sync: false
      - key: NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY
        sync: false
      - key: STRIPE_SECRET_KEY
        sync: false
      - key: STRIPE_WEBHOOK_SECRET
        sync: false
      - key: NEXT_PUBLIC_SITE_URL
        sync: false

  - type: redis
    name: innovation-redis
    region: oregon
    plan: free
    ipAllowList: []

databases:
  - name: innovation-db
    databaseName: innovation
    user: innovation
    region: oregon
    plan: free
