import { redirect } from 'next/navigation';
export default function NotificationsPage({ params }: { params: { tenant: string } }) { redirect(`/${params.tenant}/dashboard/management?tab=notifications`); }