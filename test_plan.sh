echo '1. Modify `apps/frontend/components/AnnouncementBanner.tsx` to add accessibility attributes to the close button.'
echo '   - I will use the replace_with_git_merge_diff tool to replace the `<button>` tag with `aria-label="Close announcement"`, `title="Close announcement"`, and add focus styles `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-current`.'
